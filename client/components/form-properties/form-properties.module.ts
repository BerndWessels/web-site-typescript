/**
 * This is a component that helps you dynamically create forms with row and col span.
 */
((): void => {
  'use strict';
  // register the module and it's dependencies.
  angular.module('formProperties', [
    'formLayout'
  ]);
})();
